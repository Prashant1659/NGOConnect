<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Verify NGOs | NGO Connect</title>
  <!-- Google Fonts -->
  <link href="https://fonts.googleapis.com/css2?family=Inter:wght@300;400;500;600;700&display=swap" rel="stylesheet">
  <!-- Font Awesome -->
  <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css">
  <style>
    :root {
      --primary: #4361ee;
      --primary-light: #4895ef;
      --secondary: #3f37c9;
      --accent: #f72585;
      --success: #4cc9f0;
      --warning: #f8961e;
      --danger: #ef233c;
      --dark: #1a1a2e;
      --light: #f8f9fa;
      --gray: #6b7280;
      --light-gray: #e5e7eb;
      --card-radius: 16px;
      --section-radius: 20px;
      --transition: all 0.25s cubic-bezier(0.4, 0, 0.2, 1);
    }
    
    * {
      margin: 0;
      padding: 0;
      box-sizing: border-box;
      font-family: 'Inter', -apple-system, BlinkMacSystemFont, sans-serif;
    }
    
    body {
      background-color: #f8fafc;
      color: var(--dark);
    }
    
    /* Glassmorphism Effect */
    .glass-card {
      background: rgba(255, 255, 255, 0.85);
      backdrop-filter: blur(12px);
      -webkit-backdrop-filter: blur(12px);
      border-radius: var(--card-radius);
      border: 1px solid rgba(255, 255, 255, 0.18);
      box-shadow: 0 8px 32px rgba(0, 0, 0, 0.05);
    }
    
    /* Header Styles */
    .dashboard-header {
      display: flex;
      justify-content: space-between;
      align-items: center;
      padding: 18px 40px;
      background: white;
      box-shadow: 0 2px 20px rgba(0, 0, 0, 0.05);
      position: sticky;
      top: 0;
      z-index: 10;
    }
    
    .logo {
      display: flex;
      align-items: center;
      gap: 12px;
    }
    
    .logo-icon {
      width: 40px;
      height: 40px;
      background: linear-gradient(135deg, var(--primary) 0%, var(--secondary) 100%);
      border-radius: 12px;
      display: flex;
      align-items: center;
      justify-content: center;
      color: white;
      font-size: 1.2rem;
    }
    
    .logo-text {
      font-size: 1.4rem;
      font-weight: 700;
      color: var(--dark);
    }
    
    .user-nav {
      display: flex;
      align-items: center;
      gap: 30px;
    }
    
    .nav-link {
      color: var(--gray);
      text-decoration: none;
      font-weight: 500;
      display: flex;
      align-items: center;
      gap: 8px;
      transition: var(--transition);
      position: relative;
      padding: 8px 0;
    }
    
    .nav-link:hover {
      color: var(--primary);
    }
    
    .nav-link.active {
      color: var(--primary);
      font-weight: 600;
    }
    
    .nav-link.active::after {
      width: 100%;
    }
    
    .nav-link::after {
      content: '';
      position: absolute;
      bottom: 0;
      left: 0;
      width: 0;
      height: 2px;
      background: var(--primary);
      transition: var(--transition);
    }
    
    .nav-link:hover::after {
      width: 100%;
    }
    
    /* Main Content */
    .verification-container {
      max-width: 1400px;
      margin: 40px auto;
      padding: 0 30px;
    }
    
    /* Section Header */
    .section-header {
      display: flex;
      justify-content: space-between;
      align-items: center;
      margin-bottom: 30px;
    }
    
    .section-title {
      font-size: 1.8rem;
      font-weight: 700;
      color: var(--dark);
      position: relative;
    }
    
    .section-title::after {
      content: '';
      position: absolute;
      bottom: -8px;
      left: 0;
      width: 50px;
      height: 3px;
      background: linear-gradient(to right, var(--primary), var(--accent));
      border-radius: 3px;
    }
    
    /* Buttons */
    .btn {
      padding: 10px 20px;
      border-radius: 10px;
      font-weight: 600;
      cursor: pointer;
      transition: var(--transition);
      display: inline-flex;
      align-items: center;
      gap: 8px;
      text-decoration: none;
      border: none;
    }
    
    .btn-primary {
      background: linear-gradient(135deg, var(--primary) 0%, var(--secondary) 100%);
      color: white;
      box-shadow: 0 4px 12px rgba(67, 97, 238, 0.2);
    }
    
    .btn-primary:hover {
      transform: translateY(-2px);
      box-shadow: 0 6px 16px rgba(67, 97, 238, 0.3);
    }
    
    .btn-success {
      background: linear-gradient(135deg, var(--success) 0%, #0891b2 100%);
      color: white;
      box-shadow: 0 4px 12px rgba(16, 185, 129, 0.2);
    }
    
    .btn-success:hover {
      transform: translateY(-2px);
      box-shadow: 0 6px 16px rgba(16, 185, 129, 0.3);
    }
    
    .btn-danger {
      background: linear-gradient(135deg, var(--danger) 0%, #b91c1c 100%);
      color: white;
      box-shadow: 0 4px 12px rgba(239, 68, 68, 0.2);
    }
    
    .btn-danger:hover {
      transform: translateY(-2px);
      box-shadow: 0 6px 16px rgba(239, 68, 68, 0.3);
    }
    
    /* Status Badge */
    .verification-status {
      display: inline-flex;
      align-items: center;
      gap: 8px;
      padding: 8px 16px;
      border-radius: 20px;
      font-size: 0.9rem;
      font-weight: 600;
      background: rgba(248, 150, 30, 0.1);
      color: var(--warning);
    }
    
    /* NGO Cards */
    .ngos-grid {
      display: grid;
      grid-template-columns: repeat(auto-fill, minmax(380px, 1fr));
      gap: 25px;
    }
    
    .ngo-card {
      composes: glass-card;
      overflow: hidden;
      transition: var(--transition);
      position: relative;
    }
    
    .ngo-card:hover {
      transform: translateY(-5px);
      box-shadow: 0 15px 30px rgba(0, 0, 0, 0.1);
    }
    
    .ngo-header {
      padding: 24px;
      border-bottom: 1px solid rgba(0, 0, 0, 0.05);
    }
    
    .ngo-name {
      font-size: 1.3rem;
      font-weight: 700;
      margin-bottom: 5px;
      color: var(--dark);
    }
    
    .ngo-email {
      color: var(--gray);
      font-size: 0.95rem;
    }
    
    .ngo-body {
      padding: 24px;
    }
    
    .detail-row {
      display: flex;
      margin-bottom: 16px;
    }
    
    .detail-label {
      width: 140px;
      color: var(--gray);
      font-size: 0.9rem;
      font-weight: 500;
    }
    
    .detail-value {
      flex: 1;
      font-weight: 500;
      color: var(--dark);
    }
    
    .documents-list {
      margin-top: 24px;
    }
    
    .documents-title {
      font-size: 1rem;
      font-weight: 600;
      margin-bottom: 16px;
      color: var(--dark);
    }
    
    .document-item {
      display: flex;
      align-items: center;
      gap: 12px;
      padding: 12px 0;
      border-bottom: 1px solid rgba(0, 0, 0, 0.05);
    }
    
    .document-item:last-child {
      border-bottom: none;
    }
    
    .document-icon {
      color: var(--primary);
      font-size: 1.2rem;
      width: 24px;
      text-align: center;
    }
    
    .document-name {
      flex: 1;
      font-size: 0.9rem;
      color: var(--dark);
    }
    
    .view-doc {
      color: var(--primary);
      text-decoration: none;
      font-size: 0.85rem;
      font-weight: 600;
      transition: var(--transition);
    }
    
    .view-doc:hover {
      color: var(--secondary);
    }
    
    .ngo-footer {
      padding: 20px;
      background: rgba(249, 250, 251, 0.7);
      display: flex;
      justify-content: space-between;
      gap: 15px;
    }
    
    /* Responsive Design */
    @media (max-width: 1024px) {
      .ngos-grid {
        grid-template-columns: repeat(auto-fill, minmax(320px, 1fr));
      }
    }
    
    @media (max-width: 768px) {
      .dashboard-header {
        flex-direction: column;
        gap: 15px;
        padding: 15px 20px;
      }
      
      .user-nav {
        width: 100%;
        justify-content: space-around;
        gap: 10px;
        flex-wrap: wrap;
      }
      
      .verification-container {
        padding: 0 20px;
      }
      
      .section-header {
        flex-direction: column;
        align-items: flex-start;
        gap: 15px;
      }
      
      .ngo-footer {
        flex-direction: column;
      }
    }
    
    @media (max-width: 480px) {
      .ngos-grid {
        grid-template-columns: 1fr;
      }
      
      .detail-row {
        flex-direction: column;
        gap: 4px;
      }
      
      .detail-label {
        width: 100%;
      }
    }
    
    /* Animations */
    @keyframes fadeIn {
      from {
        opacity: 0;
        transform: translateY(20px);
      }
      to {
        opacity: 1;
        transform: translateY(0);
      }
    }
    
    .ngo-card {
      animation: fadeIn 0.6s ease-out forwards;
    }
    
    .ngo-card:nth-child(1) { animation-delay: 0.1s; }
    .ngo-card:nth-child(2) { animation-delay: 0.2s; }
    .ngo-card:nth-child(3) { animation-delay: 0.3s; }
    .ngo-card:nth-child(4) { animation-delay: 0.4s; }
    
    /* Floating Action Button */
    .fab {
      position: fixed;
      bottom: 30px;
      right: 30px;
      width: 56px;
      height: 56px;
      border-radius: 50%;
      background: linear-gradient(135deg, var(--primary) 0%, var(--secondary) 100%);
      color: white;
      display: flex;
      align-items: center;
      justify-content: center;
      box-shadow: 0 6px 20px rgba(67, 97, 238, 0.3);
      cursor: pointer;
      transition: var(--transition);
      z-index: 50;
      border: none;
    }
    
    .fab:hover {
      transform: translateY(-3px) scale(1.05);
      box-shadow: 0 8px 24px rgba(67, 97, 238, 0.4);
    }
    
    .fab i {
      font-size: 1.4rem;
    }
  </style>
</head>
<body>
  <!-- Dashboard Header -->
  <header class="dashboard-header glass-card">
    <div class="logo">
      <div class="logo-icon">
        <i class="fas fa-hands-helping"></i>
      </div>
      <div class="logo-text">NGO Connect</div>
    </div>
    
    <nav class="user-nav">
      <a href="/admin/dashboard" class="nav-link">
        <i class="fas fa-chart-pie"></i>
        <span>Dashboard</span>
      </a>
      <a href="/admin/manage-users" class="nav-link">
        <i class="fas fa-users-gear"></i>
        <span>Manage Users</span>
      </a>
      <a href="/admin/verify-ngos" class="nav-link active">
        <i class="fas fa-badge-check"></i>
        <span>Verify NGOs</span>
      </a>
      <a href="/logout" class="nav-link">
        <i class="fas fa-sign-out-alt"></i>
        <span>Logout</span>
      </a>
    </nav>
  </header>
  
  <!-- Main Content -->
  <div class="verification-container">
    <div class="section-header">
      <h1 class="section-title">NGO Verification</h1>
      <div>
        <span class="verification-status">
          <i class="fas fa-clock"></i> <%= ngos.length %> Pending Verifications
        </span>
      </div>
    </div>
    
    <!-- NGO List -->
    <div class="ngos-grid">
      <% ngos.forEach(ngo => { %>
        <div class="ngo-card">
          <div class="ngo-header">
            <h3 class="ngo-name"><%= ngo.organizationName %></h3>
            <p class="ngo-email"><%= ngo.email %></p>
          </div>
          
          <div class="ngo-body">
            <div class="detail-row">
              <span class="detail-label">Registration Date:</span>
              <span class="detail-value"><%= new Date(ngo.createdAt).toLocaleDateString('en-US', { year: 'numeric', month: 'long', day: 'numeric' }) %></span>
            </div>
            
            <div class="detail-row">
              <span class="detail-label">Organization Type:</span>
              <span class="detail-value"><%= ngo.organizationType.charAt(0).toUpperCase() + ngo.organizationType.slice(1) %>
              </span>
            </div>
            
            <div class="detail-row">
              <span class="detail-label">Registration Number:</span>
              <span class="detail-value"><%= ngo.registrationNumber %></span>
            </div>
            
            <div class="detail-row">
              <span class="detail-label">Location:</span>
              <span class="detail-value"><%= ngo.city %>, <%= ngo.country %></span>
            </div>
            
            <div class="documents-list">
              <h4 class="documents-title">Submitted Documents</h4>
              
              <div class="document-item">
                <i class="fas fa-file-alt document-icon"></i>
                <span class="document-name">Registration Certificate</span>
                <% if (ngo && ngo.certificate) { %>
                  <a href="<%= ngo.certificate %>" target="_blank" class="view-doc">
                    View <i class="fas fa-external-link-alt" style="font-size: 0.7rem;"></i>
                  </a>
                <% } else { %>
                  <span class="text-gray-400">Not provided</span>
                <% } %>
              </div>
              
              <div class="document-item">
                <i class="fas fa-file-invoice document-icon"></i>
                <span class="document-name">Tax Exemption Proof</span>
                <% if (ngo && ngo.documents && ngo.documents.taxExemptionProof) { %>
                  <a href="<%= ngo.documents.taxExemptionProof %>" target="_blank" class="view-doc">
                    View <i class="fas fa-external-link-alt" style="font-size: 0.7rem;"></i>
                  </a>
                <% } else { %>
                  <span class="text-gray-400">Not provided</span>
                <% } %>
              </div>
              
              <% if(ngo && ngo.documents && ngo.documents.otherDocuments) { %>
                <div class="document-item">
                  <i class="fas fa-file-pdf document-icon"></i>
                  <span class="document-name">Additional Documents</span>
                  <a href="<%= ngo.documents.otherDocuments %>" target="_blank" class="view-doc">
                    View <i class="fas fa-external-link-alt" style="font-size: 0.7rem;"></i>
                  </a>
                </div>
              <% } %>
            </div>
          </div>
          
          <div class="ngo-footer">
            <form action="/admin/approve-ngo/<%= ngo._id %>" method="POST" style="display: inline;">
              <button type="submit" class="btn btn-success" onclick="return confirm('Approve <%= ngo.organizationName %>?')">
                <i class="fas fa-check"></i> Approve
              </button>
            </form>
            
            <form action="/admin/reject-ngo/<%= ngo._id %>" method="POST" style="display: inline;">
              <button type="submit" class="btn btn-danger" onclick="return confirm('Reject <%= ngo.organizationName %>?')">
                <i class="fas fa-times"></i> Reject
              </button>
            </form>
          </div>
        </div>
      <% }) %>
    </div>
  </div>

  <!-- Floating Action Button -->
  <button class="fab" onclick="window.location.href='/admin/create-campaign'">
    <i class="fas fa-plus"></i>
  </button>
</body>
</html>