<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title><%= title || 'NGO Donation Platform' %></title>
  <!-- Bootstrap CSS -->
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
  <!-- Font Awesome -->
  <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css">
  <!-- Google Fonts -->
  <link href="https://fonts.googleapis.com/css2?family=Nunito+Sans:wght@300;400;600;700;800&family=Playfair+Display:wght@400;500;600&display=swap" rel="stylesheet">
  <style>
    :root {
      --primary-color: #2a9d8f;
      --secondary-color: #264653;
      --accent-color: #e76f51;
      --light-accent: #f4a261;
      --dark-color: #1a1a2e;
      --light-color: #f8f9fa;
      --compassion-pink: #e63946;
    }
    
    body {
      font-family: 'Nunito Sans', sans-serif;
    }
    
    .navbar-brand {
      font-family: 'Playfair Display', serif;
      font-weight: 700;
      font-size: 1.8rem;
      color: var(--secondary-color);
      display: flex;
      align-items: center;
    }
    
    .navbar-brand .heart-icon {
      color: var(--compassion-pink);
      margin-right: 8px;
      font-size: 1.5rem;
    }
    
    .nav-link {
      font-weight: 600;
      margin: 0 8px;
      color: var(--secondary-color);
      position: relative;
      font-size: 0.95rem;
      letter-spacing: 0.3px;
    }
    
    .nav-link:hover {
      color: var(--primary-color);
    }
    
    .nav-link.active {
      color: var(--primary-color);
      font-weight: 700;
    }
    
    .nav-link.active::after {
      content: '';
      position: absolute;
      bottom: -3px;
      left: 50%;
      transform: translateX(-50%);
      width: 60%;
      height: 3px;
      background: var(--primary-color);
      border-radius: 3px;
    }
    
    .btn-donate {
      background-color: var(--accent-color);
      border-color: var(--accent-color);
      color: white;
      border-radius: 8px;
      padding: 8px 22px;
      font-weight: 700;
      transition: all 0.3s ease;
      letter-spacing: 0.5px;
      text-transform: uppercase;
      font-size: 0.85rem;
    }
    
    .btn-donate:hover {
      background-color: #d45a3f;
      border-color: #d45a3f;
      transform: translateY(-2px);
      box-shadow: 0 4px 15px rgba(231, 111, 81, 0.4);
      color: white;
    }
    
    .navbar {
      padding: 12px 0;
      box-shadow: 0 2px 20px rgba(0, 0, 0, 0.08);
      background-color: white !important;
    }
    
    .dropdown-menu {
      border: none;
      box-shadow: 0 5px 25px rgba(0, 0, 0, 0.12);
      border-radius: 8px;
      padding: 8px 0;
      margin-top: 10px;
      border: 1px solid rgba(0, 0, 0, 0.05);
    }
    
    .dropdown-item {
      padding: 8px 20px;
      font-size: 0.9rem;
      color: var(--secondary-color);
      display: flex;
      align-items: center;
    }
    
    .dropdown-item:hover {
      background-color: rgba(42, 157, 143, 0.08);
      color: var(--primary-color);
    }
    
    .dropdown-item i {
      width: 20px;
      margin-right: 10px;
      text-align: center;
      color: var(--primary-color);
    }
    
    .user-avatar {
      width: 32px;
      height: 32px;
      border-radius: 50%;
      background-color: var(--primary-color);
      color: white;
      display: inline-flex;
      align-items: center;
      justify-content: center;
      margin-right: 8px;
      font-weight: bold;
      font-size: 0.9rem;
    }
    
    .btn-login {
      border-radius: 8px;
      padding: 8px 18px;
      font-weight: 600;
      border: 2px solid var(--primary-color);
      color: var(--primary-color);
      margin-right: 12px;
    }
    
    .btn-login:hover {
      background-color: rgba(42, 157, 143, 0.1);
      color: var(--primary-color);
    }
    
    .nav-pill {
      background-color: rgba(233, 106, 82, 0.1);
      color: var(--accent-color);
      padding: 4px 12px;
      border-radius: 50px;
      font-size: 0.75rem;
      font-weight: 700;
      margin-left: 8px;
      vertical-align: middle;
    }
    
    @media (max-width: 991.98px) {
      .navbar-collapse {
        padding-top: 15px;
      }
      .nav-item {
        margin-bottom: 5px;
      }
      .d-flex {
        margin-top: 15px;
        padding-top: 15px;
        border-top: 1px solid rgba(0, 0, 0, 0.05);
      }
    }
  </style>
</head>
<body>
  <nav class="navbar navbar-expand-lg navbar-light sticky-top">
    <div class="container">
      <a class="navbar-brand" href="/">
        <i class="fas fa-heart heart-icon"></i>HopeBridge
      </a>
      <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav">
        <span class="navbar-toggler-icon"></span>
      </button>
      <div class="collapse navbar-collapse" id="navbarNav">
        <ul class="navbar-nav me-auto">
          <li class="nav-item">
            <a class="nav-link <%= currentPage === 'home' ? 'active' : '' %>" href="/">Home</a>
          </li>
          <li class="nav-item">
            <a class="nav-link <%= currentPage === 'causes' ? 'active' : '' %>" href="/causes">Our Causes <span class="nav-pill">Hot</span></a>
          </li>
          <li class="nav-item dropdown">
            <a class="nav-link dropdown-toggle <%= currentPage === 'about' || currentPage === 'team' || currentPage === 'impact' ? 'active' : '' %>" href="#" id="aboutDropdown" role="button" data-bs-toggle="dropdown">
              About Us
            </a>
            <ul class="dropdown-menu">
              <li><a class="dropdown-item <%= currentPage === 'about' ? 'active' : '' %>" href="/about"><i class="fas fa-book-open"></i> Our Story</a></li>
              <li><a class="dropdown-item <%= currentPage === 'team' ? 'active' : '' %>" href="/team"><i class="fas fa-users"></i> Our Team</a></li>
              <li><a class="dropdown-item <%= currentPage === 'impact' ? 'active' : '' %>" href="/impact"><i class="fas fa-chart-line"></i> Impact</a></li>
              <li><hr class="dropdown-divider"></li>
              <li><a class="dropdown-item" href="/transparency"><i class="fas fa-hand-holding-heart"></i> Transparency</a></li>
            </ul>
          </li>
          <li class="nav-item">
            <a class="nav-link <%= currentPage === 'contact' ? 'active' : '' %>" href="/contact">Contact</a>
          </li>
          <li class="nav-item">
            <a class="nav-link <%= currentPage === 'blog' ? 'active' : '' %>" href="/blog">Stories</a>
          </li>
        </ul>
        
        <div class="d-flex align-items-center">
          <% if (user) { %>
            <div class="dropdown">
              <a class="nav-link dropdown-toggle d-flex align-items-center" href="#" id="userDropdown" role="button" data-bs-toggle="dropdown">
                <span class="user-avatar"><%= user.name.charAt(0).toUpperCase() %></span>
                <span class="d-none d-lg-inline"><%= user.name.split(' ')[0] %></span>
              </a>
              <ul class="dropdown-menu dropdown-menu-end">
                <li class="dropdown-header px-3 py-2 text-muted small">Signed in as <strong><%= user.name %></strong></li>
                <li><hr class="dropdown-divider my-1"></li>
                <% if (user.role === 'donor') { %>
                  <li><a class="dropdown-item" href="/donor/dashboard"><i class="fas fa-tachometer-alt"></i>Dashboard</a></li>
                  <li><a class="dropdown-item" href="/donor/donations"><i class="fas fa-donate"></i>My Donations</a></li>
                <% } else if (user.role === 'ngo') { %>
                  <li><a class="dropdown-item" href="/ngo/dashboard"><i class="fas fa-tachometer-alt"></i>Dashboard</a></li>
                  <li><a class="dropdown-item" href="/ngo/campaigns"><i class="fas fa-hands-helping"></i>My Campaigns</a></li>
                <% } else if (user.role === 'admin') { %>
                  <li><a class="dropdown-item" href="/admin/dashboard"><i class="fas fa-tachometer-alt"></i>Admin Panel</a></li>
                <% } %>
                <li><a class="dropdown-item" href="/profile"><i class="fas fa-user-cog"></i>Profile Settings</a></li>
                <li><hr class="dropdown-divider"></li>
                <li><a class="dropdown-item" href="/logout"><i class="fas fa-sign-out-alt"></i>Logout</a></li>
              </ul>
            </div>
            <a href="/donate" class="btn btn-donate ms-3">Donate Now</a>
          <% } else { %>
            <a href="/login" class="btn btn-login">Login</a>
            <a href="/register" class="btn btn-donate">Join Us</a>
          <% } %>
        </div>
      </div>
    </div>
  </nav>

  <!-- Bootstrap JS Bundle with Popper -->
  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>